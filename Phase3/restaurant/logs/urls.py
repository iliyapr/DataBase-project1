from django.urls import path
from .views import InsertLogView, DeleteLogView, UpdateLogView, DailyInsertLogView, DailyDeleteLogView, DailyUpdateLogView, LogEmployeeView, LogChefView, LogManagerView, LogWaiterView, LogCustomerView, LogIngredientView, LogShipperView, LogRecipeView, LogItemView, LogOrderView
urlpatterns = [
    path("insertlog/", InsertLogView.as_view(), name="insertlog-list"),
    path("insertlog/<str:pk>/", InsertLogView.as_view(), name="insertlog-detail"),
    path("deletelog/", DeleteLogView.as_view(), name="deletelog-list"),
    path("deletelog/<str:pk>/", DeleteLogView.as_view(), name="deletelog-detail"),
    path("updatelog/", UpdateLogView.as_view(), name="updatelog-list"),
    path("updatelog/<str:pk>/", UpdateLogView.as_view(), name="updatelog-detail"),
    path("dailyinsertlog/", DailyInsertLogView.as_view(), name="dailyinsertlog-list"),
    path("dailyinsertlog/<str:pk>/", DailyInsertLogView.as_view(), name="dailyinsertlog-detail"),
    path("dailydeletelog/", DailyDeleteLogView.as_view(), name="dailydeletelog-list"),
    path("dailydeletelog/<str:pk>/", DailyDeleteLogView.as_view(), name="dailydeletelog-detail"),
    path("dailyupdatelog/", DailyUpdateLogView.as_view(), name="dailyupdatelog-list"),
    path("dailyupdatelog/<str:pk>/", DailyUpdateLogView.as_view(), name="dailyupdatelog-detail"),
    path("logemployee/", LogEmployeeView.as_view(), name="logemployee-list"),
    path("logemployee/<str:pk>/", LogEmployeeView.as_view(), name="logemployee-detail"),
    path("logchef/", LogChefView.as_view(), name="logchef-list"),
    path("logchef/<str:pk>/", LogChefView.as_view(), name="logchef-detail"),
    path("logmanager/", LogManagerView.as_view(), name="logmanager-list"),
    path("logmanager/<str:pk>/", LogManagerView.as_view(), name="logmanager-detail"),
    path("logwaiter/", LogWaiterView.as_view(), name="logwaiter-list"),
    path("logwaiter/<str:pk>/", LogWaiterView.as_view(), name="logwaiter-detail"),
    path("logcustomer/", LogCustomerView.as_view(), name="logcustomer-list"),
    path("logcustomer/<str:pk>/", LogCustomerView.as_view(), name="logcustomer-detail"),
    path("logingredient/", LogIngredientView.as_view(), name="logingredient-list"),
    path("logingredient/<str:pk>/", LogIngredientView.as_view(), name="logingredient-detail"),
    path("logshipper/", LogShipperView.as_view(), name="logshipper-list"),
    path("logshipper/<str:pk>/", LogShipperView.as_view(), name="logshipper-detail"),
    path("logrecipe/", LogRecipeView.as_view(), name="logrecipe-list"),
    path("logrecipe/<str:pk>/", LogRecipeView.as_view(), name="logrecipe-detail"),
    path("logitem/", LogItemView.as_view(), name="logitem-list"),
    path("logitem/<str:pk>/", LogItemView.as_view(), name="logitem-detail"),
    path("logorder/", LogOrderView.as_view(), name="logorder-list"),
    path("logorder/<str:pk>/", LogOrderView.as_view(), name="logorder-detail"),
]
